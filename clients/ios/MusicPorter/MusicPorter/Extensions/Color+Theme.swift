import SwiftUI

extension Color {
    /// Music Porter brand colors matching the web dashboard dark theme.
    static let mpPrimary = Color.blue
    static let mpSuccess = Color.green
    static let mpWarning = Color.orange
    static let mpDanger = Color.red
    static let mpInfo = Color.cyan
    static let mpMuted = Color.gray
}
