Version 2.38.8 (2026-03-02):
• Updated rules
• Add merge-to-dev SRS requirements and standalone script
• Preserve group name when linking destination to named group
• Add destination group rename and composition-based icons

Version 2.38.6 (2026-03-02):
• Add cut-release.py with automation flags, recovery menus, and SRS doc

Version 2.38.4 (2026-03-02):
• Fix cancelled sync re-downloading files already on disk
• Sync client updated to v1.5.2

Version 2.38.2 (2026-03-02):
• Don't show USB in recents
• Fix pre-existing Pyright type errors in porter_core.py and web_ui.py
• Quick fix to usb destinations not showing up
• Fix GUI freeze during large USB sync by using async fs ops
• Add standalone merge-dev-to-main release script
• Build Script from Sonnet - realtime
• Sync client updated to v1.5.1

Version 2.38.0 (2026-03-01):
• Destination-centric sync — complete rewrite of sync UX across all clients (web, CLI, GUI, iOS) to focus on named destinations instead of sync keys
• DB schema migration v7→v8 — sync keys migrated to UUIDs, playlists and destinations moved from config.yaml to SQLite database
• Config schema migration v3→v4 — playlists and destinations removed from config.yaml (now stored in DB)
• Destination resolution — server auto-creates destinations on first sync via POST /api/sync/destinations/resolve
• Sources page — new playlist browsing page in sync-client GUI with freshness badges and playlist stats
• Freshness level added to GET /api/playlists response (size_bytes, duration_s)
• OpenAPI 3.1.0 specification for the server REST API (docs/openapi.yaml)
• Rewrite sync.html for destination-centric UX with destination picker and status display
• Rewrite sync-client CLI destinations/status/sync commands for destination-centric model
• Rewrite sync-client GUI sync, destinations, and sources pages
• Rewrite iOS sync status view, API client, and models for destination-centric sync
• GitHub Actions workflow for automated sync-client releases
• Fix sync status showing 'undefined' as destination name on dashboard
• Fix sync status detail endpoint returning empty playlist breakdown
• Fix cancel UI: reset pipeline state after aborting SSE stream
• Fix Process All: pass auto flag to pipeline endpoint
• Fix link endpoint crash when sync_key is null in JSON body
• Add update-and-start script with optional branch parameter
• SRS-22: Sync Functionality requirements added
• Documentation updates: DB_SCHEMA.md, SERVER_API.md rewritten with field-level docs
• iOS app version bumped to 1.7.0
• Sync client version bumped to 1.5.0

Version 2.37.3 (2026-02-28):
• iOS connection health monitor — auto-detects server loss via periodic pings, transitions to offline mode after 2 consecutive failures, auto-reconnects every 15s
• iOS manual offline mode — "Go Offline" button in Settings enters offline mode without auto-reconnect, "Reconnect" button with spinner to restore connection
• Fix iOS OfflineBanner blocking navigation bar back button — switch from safeAreaInset to VStack layout
• iOS app version bumped to 1.6.1

Version 2.37.2 (2026-02-28):
• iOS offline mode — tri-state connection model (connected/offline-with-cache/disconnected) shows cached playlists, tracks, and playback when server is unreachable
• Replace iOS download system with cache-only model — unified prefetch and pin-based caching replaces per-file download manager
• Background prefetch service with progress UI and pin/unpin triggers
• Server-side ETag support for playlist and file list endpoints — conditional requests reduce bandwidth
• Client-side ETag support in APIClient with MetadataCache for persistent API response caching
• Consolidate cache code into shared cache/ module (iOS Swift actors + sync-client TypeScript)
• Sync-client GUI improvements — app name, disk space display, sidebar layout, auto-offline mode
• Add recent destinations dropdown to sync-client Sync page
• Add server release notes to iOS and sync-client About pages
• iOS app version bumped to 1.6.0
• Sync client version bumped to 1.4.0

Version 2.37.1 (2026-02-28):
• Local audio cache — pin playlists for offline sync with background prefetch, smart eviction, and stale file detection
• Fix prefetch eviction cycling — pre-filter downloads by cache capacity so files that would be immediately evicted are never downloaded
• Cache badge indicators — red when incomplete (pinned playlists missing files), yellow when near full (<10% remaining)
• Cache size change triggers prefetch — adjusting the max cache size immediately starts a prefetch to fill or trim the cache
• Improve Sync page layout — lift destination state to Zustand, improve playlist card consistency
• Persist window size and position across restarts
• ConfigManager caching with auto-invalidation via on_change callback
• Add created_at and updated_at timestamps to file list API response
• Sync client version bumped to 1.3.0

Version 2.37.0 (2026-02-28):
• Server-only architecture — removed CLI, simplified to server-only entry point with REST API and web dashboard
• Library restructure — MP3s stored as UUID-named files in flat library/audio/, cover art in library/artwork/, all metadata in TrackDB (SQLite)
• TagApplicator — profile-specific ID3 tags applied on-the-fly during sync and download, library files stay clean with UUID-only tags
• Template-based output profiles — configurable ID3 tag templates, filename patterns, directory structures, and artwork sizing per profile
• Desktop sync client — cross-platform Electron app with USB auto-detection, hotplug monitoring, per-drive auto-sync, and incremental sync via manifest
• Sync client CLI — mporter-sync command-line tool for headless sync operations
• Cookie refresh via embedded WebView — sign into Apple Music directly in the sync client, cookies extracted and uploaded to server automatically
• Playlist file counts and sync status badges in sync client
• Library audit with duplicate source path detection
• Fix all-playlists pipeline crash, scheduled pipeline crash, and various conversion/backfill errors
• iOS app updated for API v2 with dual-URL connection support and simplified USB export
• iOS app version bumped to 1.5.0
• Sync client version bumped to 1.2.0

Version 2.36.0 (2026-02-27):
• iOS direct export — download MP3s to device and export to USB drives directly from the iOS app (SRS-20)
• iOS background downloads — downloads continue when the app is backgrounded or locked via background URLSession (SRS-21)
• Concurrent downloads — 6x throughput improvement with batched state store writes
• Fix MiniPlayer artwork for server tracks using authenticated fetch instead of unauthenticated AsyncImage
• Add local/server indicators and authenticated artwork loading to playlist detail view
• Compact TrackRow layout with icon and file size in artist row
• Fix resumeIfStalled() with duration-based stall detection and generation guard
• Fix foreground download resume and streaming ZIP memory usage
• iOS app version bumped to 1.3.0

Version 2.35.1 (2026-02-26):
• Scheduler persistence — schedule state (next_run_time, last_run_time, on_missed policy) persists in DB across server restarts via new scheduled_jobs table
• Time-of-day scheduling — optional run_at (HH:MM) for intervals >= 24h targets a specific local time instead of drifting on restart
• Configurable missed-run behavior — choose "run immediately" or "skip to next" when a scheduled time is missed (server was down)
• Fix spurious "unknown key" warnings for scheduler, server_name, and log_retention_days in config verify
• Rename merge-to-main skill to merge-dev-to-main for clarity

Version 2.35.0 (2026-02-26):
• Fix next dev version after release to use PATCH increment (e.g., 2.34.0 → 2.34.1-dev) instead of MINOR (2.35.0-dev), preserving ability to do patch releases

Version 2.34.0 (2026-02-26):
• Optimize merge-to-main skill — reduced from 17 steps to 6, cut user prompts from 7 to 1-2, removed expensive diff-scanning and /todo skill invocations
• Optimize merge-to-dev skill — reduced from 7 steps to 4, removed redundant negative section and Co-Authored-By reminders

Version 2.33.0 (2026-02-26):
• Add iOS version bump check to merge-to-main workflow — automatically detects ios/ file changes and prompts for iOS version bump
• Bump iOS app version to 1.2.0 (retroactive for v2.32.0 iOS changes)
• Mark all EQ preset SRS requirements as tested (entry 19)

Version 2.32.0 (2026-02-26):
• Equalizer presets — four audio effects (loudnorm, bass boost, treble boost, compressor) configurable per output profile and playlist
• iOS app refactor — simplified to 3 tabs (Library, Process, Settings) with guided flow for Browse → Add → Process → Export
• iOS persistent reconnect — retry loop with reconnecting UI and cancel support that preserves saved server
• File permission hardening — restrict data directory to owner-only permissions with Docker-compatible best-effort fallback
• Resolve 5 Swift compiler warnings (unused variable, unnecessary await, unused results, Swift 6 concurrency)
• Update Xcode project settings from Xcode 16.2
• iOS app version bumped to 1.2.0

Version 2.31.0 (2026-02-26):
• Migration audit logging — schema and data migrations now record deferred audit entries that are flushed to the audit trail after AuditLogger is instantiated
• Log rotation — Logger now uses daily filenames (YYYY-MM-DD.log) instead of per-session files
• Log pruning — new prune_logs() deletes log files older than configurable retention period (default 7 days)
• Log retention days setting added to web UI Settings page
• Dev version build metadata — merge-to-dev appends +<git-hash> to dev versions using SemVer build metadata format

Version 2.30.0 (2026-02-26):
• Schema versioning for config.yaml and SQLite database with sequential migration functions
• CONFIG_SCHEMA_VERSION and DB_SCHEMA_VERSION constants with migrate_config_schema() and migrate_db_schema()
• Migrations run at startup before any DB or ConfigManager is instantiated
• 3-tier branch workflow: feature → dev → main with persistent dev integration branch
• New /merge-to-dev skill for fast, automatic merges into dev (no prompts, no SRS validation)
• Rewritten /merge-to-main skill: merges dev→main with full validation, version bump, tagging, and branch cleanup
• SRS files now remain in SRS/ permanently — no longer archived after merge

Version 2.29.0 (2026-02-26):
• Client-side sync: sync playlists to browser-local folders via File System Access API (Chromium) or download as ZIP (all browsers)
• Local sync manifest (.music-porter-sync.json) persists sync key and file maps for incremental syncs across sessions
• web-client:// destination type auto-registered on first browser sync, with Client badge in destinations table
• Destination linking: optional sync_key field lets multiple destinations share a single tracking key
• ConfigManager.ensure_destination() centralizes auto-creation with optional sync_key linking
• Sync key rename: CLI --rename-key, API POST /api/sync/keys/<key>/rename, and web UI pencil button with modal
• Destination rename: CLI --rename-dest, API POST /api/sync/destinations/<name>/rename, and web UI pencil button
• Browser-sync buttons on Sync Keys and Playlist Breakdown tables for quick per-key/per-playlist client sync
• Client-side sync abort button and ETA display during File System Access sync
• Audit log UI: collapse consecutive duplicate entries, incremental page filling, and improved pagination accuracy

Version 2.28.0 (2026-02-26):
• Complete unification of sync destinations: remove all legacy USB-specific code paths and aliases
• Rename USBSyncTracker to SyncTracker, USBManager remnants cleaned up across CLI, web API, and iOS
• Remove deprecated /api/usb/* routes, sync-usb and sync-status CLI aliases, and usb_sync.html template
• Simplify web API from dual USB+sync endpoints to single unified /api/sync/* routes
• Realtime sync tracking: record each file immediately during sync instead of batch-recording after completion
• Add SyncTracker.record_file() for per-file tracking and SyncManager._extract_sync_info() helper
• iOS companion app updated with renamed model types and API paths
• Net reduction of ~400 lines across the codebase

Version 2.27.1 (2026-02-25):
• Fix sync buttons disabled on page load due to race condition in destination metadata loading

Version 2.27.0 (2026-02-25):
• Add sync action buttons to sync keys and playlist breakdown tables (web and iOS)
• Web: sync buttons in keys table and playlist detail rows with availability-based enable/disable
• iOS: swipe-to-sync on key and playlist rows using OperationViewModel with ProgressPanel
• Dashboard sync status labels and icons updated for destination types (USB vs saved)
• Conditional sync key icons on sync page based on destination type
• Auto-save custom paths as sync destinations for reuse (CLI interactive, CLI --dest, web API)
• Remove dead sync_status.html template (replaced by unified sync.html)

Version 2.26.0 (2026-02-25):
• Generalize USB sync into unified sync destinations: saved named paths, USB drives, and custom paths
• Add SyncDestination dataclass and ConfigManager CRUD (add/remove/get destinations in config.yaml)
• Rename USBManager to SyncManager with backwards-compatible aliases
• New unified `sync` CLI subcommand with --dest, --list-destinations, --add-dest, --remove-dest, --status flags
• Hidden aliases: sync-usb and sync-status preserved for backwards compatibility
• Pipeline --sync-dest flag for syncing to named destinations after processing
• 10 new /api/sync/* REST API routes for destinations, sync execution, status, and key management
• Combined web UI /sync page replacing separate USB Sync and Sync Status pages
• Pipeline web template updated with sync destination picker
• iOS companion app updated: generalized model types, new sync API methods, updated labels
• All existing /api/usb/* routes and sync-usb CLI preserved for backwards compatibility

Version 2.25.0 (2026-02-25):
• Add USB sync tracking: persistent SQLite-backed records of which files have been synced to which USB drives
• New USBSyncTracker class with batch recording, per-key and per-playlist status, file-level sync maps
• CLI: sync-status subcommand with --usb-key, --delete-key, --delete-playlist, --prune, --list-keys flags
• Web: sync status page with keys table, playlist detail breakdown, per-playlist delete and stale pruning
• API: 7 new endpoints for sync status, key/playlist management, pruning, and file sync maps
• iOS: full sync status view with key/playlist browsing, swipe-to-delete, pruning, and per-file USB indicators
• Post-sync CLI summary shows tracking stats after successful USB sync
• Pipeline and sync-usb commands automatically record synced files for tracking

Version 2.24.0 (2026-02-25):
• Add persistent SQLite-backed task history (survives server restarts)
• Redesign Operations page with stats cards, filters, pagination, and clear tool
• Add CLI subcommand: ./music-porter tasks (with --stats, --status, --operation flags)
• Add 3 new API endpoints: /api/tasks/history, /api/tasks/stats, /api/tasks/clear
• Startup recovery marks stale running/pending tasks as failed on restart

Version 2.23.0 (2026-02-25):
• Add About page with version info and release notes (CLI, web, API)
• Create project release-notes.txt with full version history
• Include release-notes.txt in Docker image

Version 2.22.0 (2026-02-25):
• Add pipeline scheduler for recurring automatic playlist updates
• Fix web server startup crash
• Set Docker container timezone configuration

Version 2.21.0 (2026-02-25):
• Add pipeline summary display to web UI
• Fix pipeline summary duplicate count calculation
• Add All Playlists button
• Docker compatibility improvements for gamdl config/temp paths
• Show gamdl error output on download failure
• Change audit log entry details from inline panel to modal popup

Version 2.20.0 (2026-02-24):
• Add --behind-proxy and --proxy-count flags for reverse proxy support
• Improved reverse proxy header handling for X-Forwarded-* headers

Version 2.19.1 (2026-02-24):
• Separate web API routes into web_api.py Blueprint
• Add audit trail, data directory, and AppContext documentation

Version 2.19.0 (2026-02-24):
• Add data directory management with auto-migration from project root
• Implement AuditLogger class with SQLite backend
• Add Audit Log page to web UI with filtering, pagination, and stats
• Wire audit logging into all business logic operations
• Auto-select target directory when source is picked in tag reset
• Fix iOS auto-reconnect self-cancellation bug
• Add server_name config option

Version 2.18.1 (2026-02-24):
• Fix QR scanning via reverse proxy
• Show external URL instead of host:port in iOS Settings when configured
• Fix logger PermissionError crash

Version 2.18.0 (2026-02-24):
• Add QR code authentication for web UI and iOS app pairing
• Add external_url config option
• Add config get/set commands

Version 2.17.0 (2026-02-24):
• Add config CLI subcommand with verify and reset actions
• Validate config.yaml integrity
• Change basic profile directory_structure to nested-artist-album

Version 2.16.0 (2026-02-24):
• Add duplicate detection to list diff command
• Identify duplicate tracks across playlists

Version 2.15.0 (2026-02-24):
• Add list CLI command with unconverted and diff subcommands
• Fix unconverted count using file-level matching

Version 2.14.0 (2026-02-24):
• Add clickable unconverted files count on web dashboard
• Show conversion UI when clicking unconverted count

Version 2.13.0 (2026-02-23):
• Add iOS music playback with mini player
• Enable HTTP Range requests for MP3 streaming
• Add swipe-down gesture to dismiss mini player

Version 2.12.0 (2026-02-23):
• Add Apple Music playlist drill-down with track listing (iOS)
• Fix single-playlist pipeline skipping download via web/iOS
• Rename Pipeline tab to Process for iOS UX
• Fix iOS app icon by adding Assets.xcassets to Xcode project

Version 2.11.0 (2026-02-23):
• Add delete playlist data feature across CLI, web, and iOS
• Delete source M4A and/or export MP3 files with optional config removal
• Fix web dashboard Reset Tags always cancelling immediately
• Disable USB export features in server mode
• Add login screen for web dashboard in server mode

Version 2.10.0 (2026-02-23):
• Add Dockerfile and docker-compose.yml
• Set gamdl config and temp paths for Docker compatibility
• Slim CLAUDE.md from 54K to 19K, extract iOS docs

Version 2.9.0 (2026-02-23):
• Add iOS companion app with native SwiftUI interface
• Server mode with API key authentication and Bonjour discovery
• File serving endpoints for iOS app

Version 2.7.0 (2026-02-23):
• Archive SRS 11 v2 (Web Dashboard API contracts and setup)
• Add djLint for Jinja2/HTML template linting

Version 2.6.0 (2026-02-23):
• Add graduated freshness levels in summary display
• Categorize playlists as Current (0 days), Recent (7 days), Stale (30+ days)

Version 2.5.3 (2026-02-23):
• Remove stale documentation and update .gitignore

Version 2.5.2 (2026-02-23):
• Add Ruff linter for Python and PyMarkdown for Markdown linting
• Configuration in pyproject.toml

Version 2.5.0 (2026-02-22):
• Move output profiles from hardcoded constants to config.yaml
• Add profile configuration system (ride-command, basic, custom)
• Bootstrap dependency check before config loading

Version 2.4.0 (2026-02-22):
• Extract business logic from CLI into service layer
• Modular design with 21+ classes and separation of concerns

Version 2.3.0 (2026-02-20):
• Add configurable output directory structure and filename format
• Profile-scoped export directories: export/<profile>/<playlist>/

Version 2.2.0 (2026-02-19):
• Add web UI progress bars for operations
• Per-file log streaming via Server-Sent Events (SSE)
• Real-time progress display

Version 2.1.0 (2026-02-19):
• Add Library Stats section to web dashboard
• Rename Playlist Summary section

Version 2.0.0 (2026-02-19):
• Add web dashboard with Flask backend
• Full CLI feature parity in web UI
• Interactive playlist management
• Move library summary onto dashboard
• Improve cookie refresh for web UI

Version 1.8.0 (2026-02-19):
• YAML config system for playlists and settings
• Profile-scoped export directories

Version 1.7.0 (2026-02-18):
• Add --output-type global flag
• OutputProfile registry system for multiple export format profiles

Version 1.6.1 (2026-02-18):
• Minor patch fixes

Version 1.6.0 (2026-02-18):
• Add resize cover art to interactive menu
• Cover art dimension management

Version 1.5.3 (2026-02-18):
• Add Art/Mod column to summary playlist table
• Add Girls Night Out playlist to config

Version 1.5.2 (2026-02-18):
• Consolidate dependencies into single requirements.txt
• Auto-venv setup
• Remove deprecated legacy scripts

Version 1.5.0 (2026-02-18):
• Cover art embed functionality with SHA-256 hash tracking
• Original cover art stored in TXXX:OriginalCoverArtHash with hard-gate protection
• Add --all flag for batch operations
• Increase default max workers from 4 to 6

Version 1.4.0 (2026-02-18):
• Add tqdm progress bars for all processing operations
• Real-time progress display during conversions and downloads

Version 1.3.1 (2026-02-18):
• Cookie auto-clean patch for non-Apple domain stripping

Version 1.3.0 (2026-02-18):
• Add cookie auto-clean on startup (strip non-Apple domains)
• Multi-threaded conversion support
• Add git tag requirement to version management workflow
