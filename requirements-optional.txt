# Optional dependencies for automatic cookie extraction
# Install with: pip install -r requirements-optional.txt

# RECOMMENDED: Selenium - Browser automation for cookie extraction
# - No keychain prompts (unlike browser-cookie3)
# - Works with Chrome, Firefox, Safari, and Edge
# - Auto-downloads browser drivers via webdriver-manager
selenium>=4.16.0
webdriver-manager>=4.0.1

# Image resizing for cover art management
Pillow>=10.0.0

# Installation:
# pip install selenium webdriver-manager
#
# Usage:
# ./apple-to-ride-command download --auto-refresh-cookies --playlist 1
#
# How it works:
# 1. Launches browser in headless mode
# 2. Navigates to music.apple.com
# 3. If not logged in, opens visible browser for user to log in
# 4. Extracts cookies from browser session
# 5. Saves to cookies.txt in Netscape format
# 6. Creates backup of old cookies (cookies.txt.backup)
